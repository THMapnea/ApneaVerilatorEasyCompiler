# ApneaVerilatorEasyCompiler
A simple open source GUI application that makes easier to compile file for verilator skipping the repeatitive task of writing manual commands and allowing easier handling of the debug process.
